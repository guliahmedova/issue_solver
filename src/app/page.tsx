import AuthSidebar from "@/features/AuthSidebar";
import LoginForm from "@/features/Login";

const Home = () => {
  return (
    <AuthSidebar>
      <LoginForm />
    </AuthSidebar>
  );
};

export default Home;
